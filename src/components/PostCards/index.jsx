export const PostCard = (props) => {
    console.log(props)
    return (
        <h1>Salve seu Zé</h1>

         //Agora vou testar o commit 
        //<div className="post">
        //      <img src={post.cover} alt={post.tile} />
        //      <div key={post.id} className="post-content">
        //        <h1> {post.title} </h1> <p> {post.body}</p>
        //      </div>
        //    </div>
    );
}